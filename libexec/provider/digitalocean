#!/bin/sh

_DO_META_ADDR=http://169.254.169.254/metadata/v1/

check() {
    [ -z "$(curl "$_DO_META_ADDR/id")" ] && return 0
}

configure() {
    NAME="$(curl "$_DO_META_ADDR/hostname")"
    DATA="$(curl "$_DO_META_ADDR/user-data")"
    KEYS="$(curl "$_DO_META_ADDR/public-keys")"
}
